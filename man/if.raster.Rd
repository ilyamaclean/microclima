% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solartools.R
\name{if.raster}
\alias{if.raster}
\title{Flexible conversion to raster object}
\usage{
if.raster(x, r)
}
\arguments{
\item{x}{an R object}

\item{r}{an R object}
}
\value{
if \code{r} is a raster, \code{x} is converted to a raster with the same attributes as \code{r}, otherwise returns \code{x}
}
\description{
\code{if.raster} is used to permit flexibility in the use of rasters, matrices or arrays in many functions.
}
\examples{
r <- is.raster(dtm100m)
r1 <- if.raster(r, dtm100m)
r2 <- if.raster(r, r)
class(r1) # is a RasterLayer
class(r2) # is a matrix
}
